enum GiftStatus { locked, unlocked, redeemed } 

class AppConstants {
  static const String redeemedMessage = "This gift has already been redeemed.";
  static const String lockedMessage = "QR not yet unlocked.";
}